import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('should render the app successfully', () => {
    // A simple test to verify the test suite is running
    expect(true).toBe(true);
  });
});
